{
  "name": "rubik-dos",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compile": "cd c; nasm mandel.asm -fbin -o boot.bin; cd -",
    "start-qemu": "qemu-system-i386 -drive format=raw,file=c/boot.bin",
    "dosbox": "open -n dosbox.app --args -c \"mount c `pwd`/c\" -c 'set PATH=C:\\NC;c:\\borlandc\\bin\\;%PATH%' -c c:",
    "make-com": "cp c/boot.bin c/mandel.com",
    "make-img": "dd if=/dev/zero of=c/boot.img bs=1024 count=1440; dd if=c/boot.bin of=c/boot.img conv=notrunc",
    "build": "npm run compile; npm run make-com; npm run make-img",
    "start": "npm run build && npm run start-qemu"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/encse/rubik-dos.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/encse/rubik-dos/issues"
  },
  "homepage": "https://github.com/encse/rubik-dos#readme",
  "devDependencies": {}
}
